Instalar node.js
Instalar postgreeSQL
npm install no terminal na pasta do backend onde está os dois .json e na pasta do reactvite onde está o outro .json para instalar os pacotes

code runner para executar o nodejs e npm run dev para executar pelo react na pasta dele

* feito apenas uma vez
npm init -y // para criar package.json